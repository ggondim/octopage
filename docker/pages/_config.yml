# Jekyll is only acting as the web server here — the site is already built by
# Astro. Processing is disabled so Jekyll cannot rewrite, exclude or re-render
# anything in dist/ and give a misleading preview.
source: /srv/site
destination: /srv/site
incremental: false
safe: false
plugins: []
exclude: []
keep_files: ['.']
