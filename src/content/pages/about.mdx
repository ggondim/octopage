---
title: About
description: A page served from the site root rather than under a section.
---

Files under `src/content/pages` map to the site root, so this one is served at
`/about`. Every other directory under `src/content` keeps its name as the URL
prefix — `src/content/blog/hello-octopage.mdx` becomes `/blog/hello-octopage`.
