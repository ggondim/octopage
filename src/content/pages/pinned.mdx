---
title: Pinned page
description: Published at a custom URL instead of the one its path implies.
---

This file lives at `src/content/pages/pinned.mdx`, which would put it at
`/pinned`. The `routes` entry in `octopage.config.ts` pins it to `/custom-place`
instead, and the path-derived route is not published at all.
