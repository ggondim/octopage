---
title: About
description: A page that lives at the site root instead of under a section.
---

Files under `pages/` map to the site root, so this one is served at `/about`.
Everything under any other configured directory keeps that directory as its URL
prefix — `blog/hello-octopage.mdx` becomes `/blog/hello-octopage`.
