---
title: Pinned page
description: Published at a custom URL instead of the one its path implies.
---

This file lives at `pages/pinned.mdx`, which would normally put it at `/pinned`.
The `routes` entry in `octopage.config.ts` pins it to `/custom-place` instead,
and the path-derived route is not published at all.
